<?php

use App\Services\ToolBox; ?>

<div class="custom-alert <?= $cssClass ?>">
    <ul>
        <?php foreach (ToolBox::getFlash($type) as $message) : ?>
            <li><?= ToolBox::clean($message) ?></li>
        <?php endforeach; ?>
    </ul>
    <button class="close-btn" onclick="this.parentElement.style.display='none'">&times;</button>
</div>