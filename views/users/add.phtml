<?php

/**
 * Ce template affiche le formulaire d'ajout d'utilisateur.
 */
?>

<div class="container mt-5">
    <h2 class="mb-4 text-primary"><?= $this->clean($title ?? 'Ajouter un utilisateur') ?></h2>

    <div class="card shadow-lg">
        <div class="card-body">
            <form action="<?= $this->url('/users/add') ?>" method="POST">
                <div class="mb-3">
                    <label for="nickname" class="form-label">Nom d'utilisateur :</label>
                    <input type="text" id="nickname" name="nickname" class="form-control" required>
                </div>

                <div class="mb-3">
                    <label for="email" class="form-label">Email :</label>
                    <input type="email" id="email" name="email" class="form-control" required>
                </div>

                <div class="mb-3">
                    <label for="password" class="form-label">Mot de passe :</label>
                    <input type="password" id="password" name="password" class="form-control" required>
                </div>

                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-person-plus"></i> Ajouter
                </button>
            </form>
        </div>
    </div>

    <a href="<?= $this->url('/users') ?>" class="btn btn-secondary mt-3">
        <i class="bi bi-arrow-left"></i> Retour à la liste des utilisateurs
    </a>
</div>