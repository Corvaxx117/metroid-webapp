<?php if (isset($valids) && count($valids) > 0) :  ?>
    <?php foreach ($valids as $valid) : ?>
        <div class="valids" role="alert">
            <ul>
                <li><?= $valid; ?></li>
            </ul>
        </div>
    <?php endforeach; ?>
<?php endif; ?>
<?php if (isset($_SESSION['valids']) && count($_SESSION['valids']) > 0) :  ?>
    <?php foreach ($_SESSION['valids'] as $valid) : ?>
        <div class="valids" role="alert">
            <ul>
                <li><?= $valid; ?></li>
            </ul>
        </div>
    <?php endforeach; ?>
    <?php unset($_SESSION['valids']); ?>
<?php endif; ?>